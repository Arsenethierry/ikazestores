"use client";

import { Card, CardHeader } from "@/components/ui/card";

const ProductReviewForm = ({ product, orderDetails, onSubmit }) => {

    return (
        <Card className="max-w-4xl mx-auto">
            <CardHeader>
                <div className="flex items-start gap-4">
                    <Image
                </div>
            </CardHeader>
        </Card>
    )
}